{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "json",
  "description": "Minimal core permissions for the JSON tool window.",
  "windows": ["tool-json"],
  "permissions": ["core:default"],
  "platforms": ["windows"]
}
